# Local full clone of the Three.js library (site loads Three.js from CDN instead)
three.js/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Dependencies (not used by this static site, but ignore just in case)
node_modules/

# Wrangler / Cloudflare local files and secrets
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
